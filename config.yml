proxy: socks5://127.0.0.1:7890

hosts:
  - https://raw.hellogithub.com/hosts

#rule-actions:
#  - &run
#    action: command
#    run: asdsadsad



rules:
  - if: xyz in domain
    then: pass

  - if: domain ends with .cn
    then: pass

middlewares:
  - rules
  - cache
  - hosts
  - block
 